<Project>

  <!--
    Auto-generated by CI for dotnet format — do not commit the generated copy.
    Source template: .github/templates/schema-package.csproj
  -->
  <Import Project="$(MSBuildThisFileDirectory)local.props" Condition="Exists('$(MSBuildThisFileDirectory)local.props')" />
  <PropertyGroup>
    <SchemaFullBuild Condition="'$(GITHUB_ACTIONS)' != 'true' and '$(ConfigurationRefsPath)' != '' and Exists('$(ConfigurationRefsPath)')">true</SchemaFullBuild>
    <ImportDirectoryBuildProps Condition="'$(SchemaFullBuild)' != 'true'">false</ImportDirectoryBuildProps>
    <ImportDirectoryBuildTargets Condition="'$(SchemaFullBuild)' != 'true'">false</ImportDirectoryBuildTargets>
    <BaseIntermediateOutputPath>obj\$(MSBuildProjectName)\</BaseIntermediateOutputPath>
    <BaseOutputPath>obj\$(MSBuildProjectName)\bin\</BaseOutputPath>
  </PropertyGroup>

  <Import Project="Sdk.props" Sdk="Microsoft.NET.Sdk" />

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Schemas/*/*.cs" />
  </ItemGroup>

  <Import Project="$(MSBuildProjectDirectory)/../.github/templates/schema-build.props" Condition="'$(SchemaFullBuild)' == 'true'" />

  <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk" />

</Project>
