<Project>
    <!-- Analyzer settings live in Directory.Build.targets: the '.Configuration' guard
         relies on $(AssemblyName), which is not yet set this early (.props is imported
         before the SDK .props that defaults AssemblyName). This stub is kept so MSBuild
         stops the upward Directory.Build.props search here. -->
</Project>
